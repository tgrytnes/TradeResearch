---
description: Start an issue and prepare the workspace
argument-hint: <issue-number> <slug>
mode: architect
---

Start work on GitHub issue $ARGUMENTS.

1. Propose the correct feature-branch name using the issue number and slug (e.g., `feat/`, `fix/`, `chore/`).
2. (Optional) Use `memory-traderesearch` or `codeix-traderesearch` MCPs only if you need deeper context for this specific task.
3. Draft a brief implementation plan (scope, touched files, tests). Do not overcomplicate for small tasks.
