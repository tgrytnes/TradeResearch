---
description: Persist a durable implementation lesson into docs and project memory
argument-hint: <topic>
mode: ask
---

Capture durable learning for $ARGUMENTS.

Workflow:

1. Identify whether the learning belongs primarily in:
   - a change record
   - an architecture note
   - a runbook
   - a feature doc
2. Update the repository doc first or propose the exact target file if no change should be made automatically.
3. If available, store a concise durable fact in `memory-traderesearch`.
4. Return:
   - what was recorded
   - where it was recorded
   - how future engineers should retrieve it
